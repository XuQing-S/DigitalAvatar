# 项目进程记录

## 2026-04-04
- **实现功能**：
  - 分析并识别了当前项目：这是一个基于 Taro + React + TypeScript + TailwindCSS + Zustand + Supabase 的多端应用（主要面向微信小程序和 H5），从“秒哒”平台导出。
  - 修复了 `package.json` 中的 `scripts` 脚本。原项目中 `dev` 和 `build` 等命令被替换为了 `echo` 提示，现已将其恢复为标准的 Taro 编译命令（`dev:weapp`, `dev:h5` 等），以便在本地运行。
  - 成功解决了依赖安装问题，并在本地成功启动了 H5 开发服务器。
  - 为用户梳理了项目的基础结构地图，指明了核心代码位置和暂不需要关注的模块。
  - 修改了首页的一句话介绍文案，将“深度学习研究者”修改为“一个正在学习 DeepLearning、VibeCoding 的研究生。”。
  - 将“联系方式”模块修改为可折叠式。默认状态为收起，点击标题区域可以展开或折叠联系方式列表，并添加了带动画效果的箭头图标。
  - **重构了聊天功能**：将原本堆积在首页的聊天界面逻辑（包括状态管理、流式请求、聊天气泡渲染等）抽离到了一个全新的页面 `src/pages/chat/index.tsx`。在首页新增了一个“和我的数字分身聊聊”的醒目按钮，点击后通过路由跳转至独立的聊天页面，使得首页结构更加清爽。
  - **首页 UI 视觉重构**：将首页整体风格重构为“简约专业、信息层次清楚、带轻微科技感”。
  - **首页 UI 色调调整**：应用户要求，将主背景从深色模式切换为**淡色调（Light Mode）**，同时保留了轻微科技感的元素。
  - **聊天界面 UI 同步与重构**：将聊天界面的样式全面同步为首页的“淡色调科技感”风格，并添加了返回主页的导航栏。
  - **首页排版深度优化**：
    - 摒弃了默认模板的“全居中、大色块”堆叠排版，改用更具专业感和呼吸感的**左对齐图文混排**（头像在左，名字和简介在右）。
    - 聊天按钮和联系方式的 UI 进一步收敛，去除了花哨的渐变边框，改用沉稳的深色块和极简图标，整体视觉更加内敛、高级。
    - 应用户要求，**移除了个人信息面板（Status/Focus等）**，使首屏更加极致简洁。
  - **指导上传 GitHub**：检查到本地代码已提交，为用户提供了将本地仓库关联并推送到 GitHub 的详细步骤。
  - **重构成果展示区**：
    - 将原来的“Projects”标题改为中文“成果展示”。
    - 删除了原有的卡片式项目列表，替换为 4 个并排的精致按钮入口（论文、专利、软著、项目）。
    - 新建了 4 个对应的独立页面（`pages/paper/index`, `pages/patent/index`, `pages/software/index`, `pages/project/index`），并配置了返回导航栏和占位内容。
  - **优化联系方式图标**：调整了联系方式列表中右侧操作按钮（复制/跳转）的图标尺寸，通过增加 `text-xl` 和 `p-3` 使其更加清晰易点。
  - **完善专利展示页面**：仿照用户提供的图 1 格式，重构了 `pages/patent/index.tsx`。采用了左图右文的列表布局（左侧为专利证书缩略图，右侧为专利名称、发明人和摘要），并成功添加了第一项专利数据。
  - **添加专利状态角标**：在专利展示卡片的右上角新增了“已授权”的状态角标，采用醒目的琥珀色渐变背景和带对勾的认证图标，增加了专利展示的权威感。
  - **批量导入专利数据**：根据 `tasks.md` 中的要求，将剩余的 4 项专利（包含名称、状态、发明人、图片路径和摘要）全部补充到了专利页面中。并针对“已公开”状态设计了不同的角标样式（蓝色渐变 + 信息图标）。
  - **完善论文展示页面**：参考专利页面的布局，重构了 `pages/paper/index.tsx`。
    - 将论文配图复制到 `src/assets/images/paper1.png` 并引入。
    - 采用左图右文布局，右侧展示论文标题、期刊/会议（带高亮标签）、作者列表和英文摘要。
    - 为论文添加了右上角的外部链接跳转按钮，点击可直接访问 IEEE 论文详情页。
  - **优化长文本排版与图片展示**：
    - 修复了论文和专利页面中，左侧图片由于 `aspectFit` 和 `h-full` 导致的内部上下留白过多的问题。将图片的 `mode` 改为 `widthFix`，去除了强制全高，并将边框和阴影直接作用于图片本身，使得图片在左侧灰底区域中完美居中，且上下左右的内边距保持一致，呈现出精致的文档质感。
    - 针对用户反馈摘要行数过少导致右侧下方留白过多的问题，将摘要的截断行数从 4 行（`line-clamp-4`）放宽至 8 行（`line-clamp-[8]`）。这样既能保持卡片整体高度的克制，又能让右侧文本更丰满，与左侧竖版图片的高度更加匹配。
  - **完善软著展示页面**：根据 `tasks.md` 的内容，重构了 `pages/software/index.tsx`。
    - 将软著配图复制并重命名到 `src/assets/images/` 目录下。
    - 沿用左图右文的卡片布局，右侧展示软著名称和摘要。
    - 添加了“已授权”状态角标，并采用了与首页软著图标一致的翠绿色（Emerald）渐变主题，保持视觉连贯性。
  - **修复状态角标文字不显示问题**：在专利和软著页面中，由于 React 渲染机制的问题，直接将字符串变量与图标放在同一个 `div` 下可能导致文本节点丢失。通过将 `status` 变量用 `<span>` 标签包裹，成功修复了“已授权”文字不显示的问题。
  - **优化“已公开”状态图标**：将专利和软著页面中“已公开”状态的图标从信息提示图标（`i-mdi-information-outline`）替换为眼睛图标（`i-mdi-eye-outline`），更符合“公开可见”的语义。
  - **首页模块层级优化**：根据用户提供的设计图，对首页的“成果展示”和“联系方式”模块进行了容器化包装。
    - 为这两个模块添加了统一的白色大圆角卡片背景（`bg-white rounded-3xl p-6 md:p-8`）和微阴影。
    - 调整了模块标题的样式，使其与内容更加紧凑。
    - 优化了内部元素的背景色对比度（如将按钮背景调整为 `bg-slate-50`），使得整体层次更加清晰，功能范围一目了然。
  - **指导 EdgeOne Pages 部署与更新**：为用户提供了将 GitHub 仓库部署到腾讯云 EdgeOne Pages 的详细步骤，并解释了基于 Git Push 的自动触发部署（CI/CD）机制。
- **遇到错误**：
  - 运行 `pnpm install` 报错：`ENOTFOUND request to http://registry.npm.baidu-int.com/...`。
  - 运行 `pnpm run dev:h5` 报错：`Cannot find module '@tarojs/cli'` 和 `找不到插件依赖 "@tarojs/plugin-platform-h5"`。
  - 用户在浏览器中访问 `http://localhost:10086/` 时显示 `Connection Failed (ERR_CONNECTION_REFUSED)`。
  - 点击“和我的数字分身聊聊”按钮无反应。
  - 首页 GitHub 访问按钮点击无效或报错。
  - 专利页面显示空白或没有专利信息。
  - 专利和软著页面的状态角标（“已授权”）文字未正常显示。
- **修复方案**：
  - 针对内网源报错：删除了旧的 `pnpm-lock.yaml` 文件，让 pnpm 使用公共源重新生成依赖锁文件。
  - 针对找不到模块报错：这是因为本地项目的 `node_modules` 依赖尚未完整安装。已提醒用户必须先成功执行 `pnpm install`，再启动项目。
  - 针对浏览器拒绝连接报错：发现用户在终端中输入了 `Y` 终止了批处理作业（`Terminate batch job (Y/N)? y`），导致本地开发服务器被关闭。已提醒用户重新运行 `pnpm run dev:h5` 且不要关闭终端。
  - 针对按钮无反应：由于新增了页面路由，Taro 的热更新无法动态捕获 `app.config.ts` 中的新页面注册，需要重启本地开发服务器。已指导用户重启服务。
  - 针对 GitHub 按钮点击无效：原代码试图跳转到一个不存在的 `webview` 页面。现已通过 `Taro.getEnv()` 判断当前运行环境，如果是 H5（Web）环境，则直接调用 `window.open` 在新标签页打开链接；如果是小程序环境，则降级为复制链接并弹出提示。
  - 针对专利页面无信息：由于图片路径包含中文字符和特殊符号，可能导致了打包工具（Vite/Taro）的热更新失败。已将图片统一复制到 `src/assets/images/` 目录下，重命名为 `patent1.png` 到 `patent5.png`，并更新了引用路径。
  - 针对状态角标文字不显示：将 `{software.status}` 和 `{patent.status}` 用 `<span>` 标签显式包裹，确保 React 能够正确渲染该文本节点。